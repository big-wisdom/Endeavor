import 'package:flutter/material.dart';

class EndeavorBlockScreen extends StatelessWidget {
  const EndeavorBlockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
