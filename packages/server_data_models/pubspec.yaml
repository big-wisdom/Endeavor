name: server_data_models
description: Package to define server side data models for the endeavor app
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <=3.10.4'

dependencies:
  data_models:
    path: ../data_models
  flutter:
    sdk: flutter
